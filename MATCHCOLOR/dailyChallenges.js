// Optional overrides keyed by date (YYYY-MM-DD). If today isn't listed, the
// daily challenge is derived deterministically from the date string itself
// (see index.js seededRandom), so nothing needs to be added ahead of time.
window.dailyChallenges = {};
